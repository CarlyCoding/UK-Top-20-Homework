import React from "react";

const MusicDetail = ({selectedMusic}) => {
    return (
        <div>
            <h3>{selectedMusic.title.label}</h3>
        </div>
    )
}







export default MusicDetail;